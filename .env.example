# Environment Variables — Server

# MongoDB Atlas connection string
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/cruise-booking?retryWrites=true&w=majority

# Express server port
PORT=5000

# Frontend origin for CORS
CLIENT_URL=http://localhost:5173
